using UnityEngine;
using ZGame.Editor.MapBuild;

namespace ZGame.Editor.ResBuild
{
    public class ResBuildSeting : SubPageScene
    {
        public override string name { get; } = "设置";

        public ResBuildSeting(Docker window) : base(window)
        {
        }

        public override void OnGUI(Rect rect)
        {
            
        }
    }
}